'use client';

import { MinimalParticleBackground } from './MinimalParticleBackground';

/**
 * BackgroundLayer Component
 * Displays the dynamic particle network background
 * Replaces the static image with a Devin.ai-inspired generative effect
 */
export function BackgroundLayer(): JSX.Element {
  return <MinimalParticleBackground />;
}

